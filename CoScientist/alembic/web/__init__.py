"""Web dashboard for the alembic pipeline.

Live split-screen view: source-repo map (from the explorer report) on the left,
the MCP server being drafted (from the coder report + validator results) on the
right, with a per-stage progress rail and a live tool-call feed in the middle.

Run:
    python -m alembic.web.server
"""
